diff --git a/README.md b/README.md
index 5c0dbf9..6677744 100644
--- a/README.md
+++ b/README.md
@@ -150,7 +150,7 @@ flowchart LR
     HSRC["host/src/*.c<br/>host/inc/*.h"]
     GLIB["guest/lib/*.c<br/>start, interrupts, irqproto,<br/>syscall, print, string"]
     GTEST["guest/tests/name.c<br/>provides guest_main()"]
-    GLD["guest/guest.ld<br/>raw binary linked at 0x8000"]
+    GLD["guest/guest.ld<br/>raw binary linked at 0x10000"]
 
     ABI --> BIN["host/build/hypervisor<br/>gcc -Wall -Wextra -O2 -pthread"]
     HSRC --> BIN
@@ -175,7 +175,7 @@ emits the same links as ELF (`guest/build/<name>.elf`) for `readelf -S`.
 ```
 usage: hypervisor -g <image> [image...] [options]
 
-  -m, --memory <2|4|8>        guest memory size in MB (default 2)
+  -m, --memory <2|4|8|16>     guest memory size in MB (default 2)
   -p, --page   <4|2>          page size, 4 for 4KB or 2 for 2MB (default 4)
   -g, --guest  <img> [img...] guest images; one VM is launched per image
   -f, --file   <f> [f...]     files shared between VMs
@@ -286,15 +286,15 @@ image is reserved for hypervisor-owned structures:
 0x002000  PDPT
 0x003000  PD
 0x004000  PT[0]  ┐  4 KB paging only. mem_size/4096 PTEs means
-0x005000  PT[1]  │  512 / 1024 / 2048 entries for 2 / 4 / 8 MB,
-0x006000  PT[2]  │  so 1 / 2 / 4 tables — exactly what fits below
-0x007000  PT[3]  ┘  the guest.
-0x008000  guest image (linked at this address), then .bss
+0x005000  PT[1]  │  512 / 1024 / 2048 / 4096 entries for 2 / 4 / 8 / 16 MB,
+   ...           │  so 1 / 2 / 4 / 8 tables; the 12 pages below the
+0x00B000  PT[7]  ┘  guest leave room for up to 24 MB.
+0x010000  guest image (linked at this address), then .bss
    ...    free
 mem_size  initial rsp; the stack grows down
 ```
 
-With 2 MB pages only PML4+PDPT+PD are used, with `PDE64_PS` set on 1 / 2 / 4 PD
+With 2 MB pages only PML4+PDPT+PD are used, with `PDE64_PS` set on 1 / 2 / 4 / 8 PD
 entries. The guest recovers `mem_size` from its initial `rsp`, rounded up to the
 next 2 MB boundary — no extra port, nothing hardcoded.
 
@@ -356,7 +356,7 @@ sequenceDiagram
 
 Pointer arguments (`path`, `buf`) are ordinary guest addresses inside the struct
 and are translated the same way, so nothing needs serializing. A 32-bit address
-suffices because `mem_size` never exceeds 8 MB; the generalisation is two `outl`s.
+suffices because `mem_size` never exceeds 16 MB; the generalisation is two `outl`s.
 
 The spec's flag values collide with POSIX — spec `O_RDWR` is 4 where POSIX says 2,
 spec `SEEK_SET` is 1 where POSIX says 0 — so the shared header uses `HV_`-prefixed
@@ -433,7 +433,7 @@ multi-round path and the barrier.
 
 Each of these had a real alternative; the alternative is the interesting half.
 
-**D1 — Identity map, guest at `0x8000`, page tables below it.**
+**D1 — Identity map, guest at `0x10000`, page tables below it.**
 `[0, mem_size)` is identity mapped, so `GVA == GPA` and translating a guest pointer
 is `v->mem + gva` plus a bounds check. *Alternative:* the `KVM_TRANSLATE` ioctl
 does GVA→GPA properly, but costs one ioctl per pointer argument and still needs the
diff --git a/common/hv_abi.h b/common/hv_abi.h
index 42099dc..8bd010e 100644
--- a/common/hv_abi.h
+++ b/common/hv_abi.h
@@ -24,7 +24,7 @@
 
 	Pointer arguments (path, buf) are just more guest addresses inside the
 	struct and are translated the same way, so nothing has to be serialized.
-	They always fit in 32 bits because mem_size never exceeds 8 MB.
+	They always fit in 32 bits because mem_size never exceeds 16 MB.
 
 	Names are HV_-prefixed because the spec's own values collide with the
 	host's <fcntl.h> and <stdio.h>: the spec's O_RDWR is 4 while POSIX O_RDWR
diff --git a/guest/guest.ld b/guest/guest.ld
index 46948b2..02299d6 100644
--- a/guest/guest.ld
+++ b/guest/guest.ld
@@ -3,9 +3,9 @@ ENTRY(_start)
 
 SECTIONS
 {
-        /* Design D1: the image is loaded at GPA 0x8000 and [0, mem_size) is
+        /* Design D1: the image is loaded at GPA 0x10000 and [0, mem_size) is
            identity mapped, so the guest is linked at its run address. */
-        . = 0x8000;
+        . = 0x10000;
 
         .start   : { *(.start) }
         .text    : { *(.text*) }
diff --git a/host/inc/vm.h b/host/inc/vm.h
index 290562d..6b5bb2f 100644
--- a/host/inc/vm.h
+++ b/host/inc/vm.h
@@ -7,7 +7,7 @@
 
 #include "fileio.h"
 
-#define GUEST_START_ADDR 0x8000
+#define GUEST_START_ADDR 0x10000
 
 /*
 	Design D1 - guest physical memory layout.
@@ -16,8 +16,8 @@
 	  0x1000            PML4
 	  0x2000            PDPT
 	  0x3000            PD
-	  0x4000 .. 0x7000  PT[0..3]        (4 KB paging only; 4 PTs == 8 MB, the maximum)
-	  0x8000            guest image, then .bss, then the stack growing down from mem_size
+	  0x4000 .. 0xB000  PT[0..7]        (4 KB paging only; 8 PTs == 16 MB, the maximum)
+	  0x10000           guest image, then .bss, then the stack growing down from mem_size
 
 	[0, mem_size) is identity mapped, so GVA == GPA and translating a guest
 	pointer in phases B and C is v->mem + gva plus a bounds check.
diff --git a/host/src/opts.c b/host/src/opts.c
index 1df90a1..b7aa24a 100644
--- a/host/src/opts.c
+++ b/host/src/opts.c
@@ -15,7 +15,7 @@ void usage(const char *prog)
 	fprintf(stderr,
 		"usage: %s -g <image> [image...] [options]\n"
 		"\n"
-		"  -m, --memory <2|4|8>        guest memory size in MB (default %d)\n"
+		"  -m, --memory <2|4|8|16>     guest memory size in MB (default %d)\n"
 		"  -p, --page   <4|2>          page size, 4 for 4KB or 2 for 2MB (default %d)\n"
 		"  -g, --guest  <img> [img...] guest images; one VM is launched per image\n"
 		"  -f, --file   <f> [f...]     files shared between VMs\n"
@@ -74,8 +74,8 @@ static int parse_memory(const char *s, int *out)
 		return -1;
 	}
 
-	if (v != 2 && v != 4 && v != 8) {
-		fprintf(stderr, "error: --memory must be 2, 4 or 8 (got '%s')\n", s);
+	if (v != 2 && v != 4 && v != 8 && v != 16) {
+		fprintf(stderr, "error: --memory must be 2, 4, 8 or 16 (got '%s')\n", s);
 		return -1;
 	}
 
diff --git a/host/src/vm.c b/host/src/vm.c
index 354f8f9..1fd5b7b 100644
--- a/host/src/vm.c
+++ b/host/src/vm.c
@@ -348,7 +348,7 @@ int vm_setup(struct vm *v, const struct vm_config *cfg)
 
 	memset(&regs, 0, sizeof(regs));
 	regs.rflags = 0x2;
-	regs.rip    = GUEST_START_ADDR;   /* GVA == GPA; the image is linked at 0x8000 */
+	regs.rip    = GUEST_START_ADDR;   /* GVA == GPA; the image is linked at GUEST_START_ADDR */
 	regs.rsp    = v->mem_size;        /* stack grows down from the top of guest memory */
 
 	if (ioctl(v->vcpu_fd, KVM_SET_REGS, &regs) < 0) {
diff --git a/scripts/demo_a.sh b/scripts/demo_a.sh
index 076f984..096979a 100755
--- a/scripts/demo_a.sh
+++ b/scripts/demo_a.sh
@@ -10,7 +10,7 @@ step $HV --memory 4 --page 2 --guest $IMG/hello.img $IMG/hello.img
 pause
 
 heading "A2  Parametric paging: every --memory / --page combination"
-for m in 2 4 8; do
+for m in 2 4 8 16; do
 	for p in 4 2; do
 		step $HV -m $m -p $p -g $IMG/mem_probe.img
 	done
diff --git a/scripts/test_phase_a.sh b/scripts/test_phase_a.sh
index c47eb42..be9079a 100755
--- a/scripts/test_phase_a.sh
+++ b/scripts/test_phase_a.sh
@@ -33,7 +33,7 @@ fi
 
 echo
 echo "2. parametric paging, six combinations"
-for m in 2 4 8; do
+for m in 2 4 8 16; do
 	for p in 4 2; do
 		out="$(run -m $m -p $p -g $IMG/mem_probe.img)"
 		if grep -q "mem_probe: ok" <<<"$out" && grep -q "($m MB)" <<<"$out"; then
@@ -56,7 +56,7 @@ check_err() {
 		bad "$desc (rc=$rc)" "$(head -1 <<<"$out")"
 	fi
 }
-check_err "-m 3 rejected"          "--memory must be 2, 4 or 8 (got '3')"   -m 3 -g $IMG/hello.img
+check_err "-m 3 rejected"          "--memory must be 2, 4, 8 or 16 (got '3')"   -m 3 -g $IMG/hello.img
 check_err "-p 8 rejected"          "--page must be 4 (4KB) or 2 (2MB)"    -p 8 -g $IMG/hello.img
 check_err "missing --guest"        "at least one guest image is required" -m 4
 check_err "unknown option"         "invalid option"                       -x
